package com.jasongrech.carlocator.ui.theme

import androidx.compose.ui.graphics.Color

// Base surfaces — near-black charcoal, stepped in flat blocks (no elevation shadows).
val Bg = Color(0xFF16171D)
val Surface = Color(0xFF1E2028)
val SurfaceRaised = Color(0xFF262938)
val Border = Color(0xFF2B2E3B)

// Text
val TextPrimary = Color(0xFFF2F3F7)
val TextSecondary = Color(0xFF8B92A3)
val TextMuted = Color(0xFF5C6270)

// Accent — warm amber, the "headlight" color. Primary actions and the parking-badge mark.
val Accent = Color(0xFFFF9F1C)
val AccentPressed = Color(0xFFE8890A)
val AccentTint = Color(0xFF3A2A14)
val OnAccent = Color(0xFF1A1400)

// Success — reserved for state, not decoration: "ready", "enabled", "saved".
val Success = Color(0xFF3BA55D)
val SuccessPressed = Color(0xFF2F8A4C)
val SuccessTint = Color(0xFF16261C)

// Danger — destructive actions only.
val Danger = Color(0xFFED4245)
val DangerPressed = Color(0xFFC93538)
val DangerTint = Color(0xFF2A1618)
